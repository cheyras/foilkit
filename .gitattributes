# The repository is LF throughout, on every platform. A byte-level receipt is
# only meaningful if the bytes do not depend on which machine checked them out.
* text=auto eol=lf
*.png binary
*.jpg binary
*.webm binary
*.sh text eol=lf
