{
  "name": "@foilkit/patterns",
  "version": "0.0.0",
  "description": "The 45 foil pattern recipes as data — a pure vec3 foilPattern(vec2 uv, vec2 tilt) per pattern, plus its uniform defaults.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "source": "./src/index.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "sideEffects": false,
  "dependencies": {
    "@foilkit/core": "workspace:*"
  },
  "scripts": {
    "build": "tsc -b"
  }
}
