node_modules/
dist/
build/
.turbo/
*.tsbuildinfo

.env
.env.*
!.env.example

.DS_Store
Thumbs.db

.vercel/
.idea/
.vscode/

# Reference media is cited, never vendored — see NOTICE-CONVENTIONS.md.
# The fetch script reproduces it locally; the pixels do not ship.
reference-media/

# Python bytecode from reference/pipeline/*.py
__pycache__/

# The stage acceptance run's screenshots and report — regenerated by
# `pnpm run acceptance`, and a screenshot is an artifact rather than a source.
apps/demo/.acceptance/
